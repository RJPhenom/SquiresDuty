// Spawn the pixelate transition that covers the room reload (same effect
// the player defeat uses — visually consistent for any death type).
instance_create_layer(0, 0, "Instances", obj_player_defeat_transition);
